# Supabase project config — only what's needed for `functions deploy`.
# We do our own auth (admin passcode / investor email+code), so the
# platform JWT check is disabled on every function.

[functions.request]
verify_jwt = false

[functions.financials]
verify_jwt = false

[functions.admin]
verify_jwt = false
